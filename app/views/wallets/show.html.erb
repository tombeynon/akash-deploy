<h1 class="display-4 mb-4">Wallet</h1>
<div class="row">
  <div class="col">
    <% if @akash.wallet.exists? %>
      <%= render "wallet", wallet: @akash.wallet %>
    <% else %>
      <p>No wallet found.</p>
    <% end %>
    <p><%= link_to 'Create new', [:new, :wallet] %></p>
  </div>
</div>
